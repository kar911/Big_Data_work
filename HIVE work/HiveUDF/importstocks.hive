DROP TABLE IF EXISTS stocks;

CREATE TABLE stocks (xchange STRING,
  symbol STRING, 
   priceDate STRING,
   open FLOAT,
   high FLOAT,
   low FLOAT,
   close FLOAT,
   volume INT,
   adjClose FLOAT) 
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

LOAD DATA LOCAL INPATH '/home/cloudera/shared/HiveUDF/NYSE_daily_prices_A.csv' OVERWRITE INTO TABLE stocks;
